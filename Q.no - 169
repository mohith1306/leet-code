from collections import Counter
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        l = Counter(nums)
        n = len(nums)
        for key,value in l.items():
            if value > (n/2):
                return key
