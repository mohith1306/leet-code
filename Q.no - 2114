class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        l = 0
        for i in sentences:
            if l<len(i.split(" ")):
                l = len(i.split(" "))
        return l
        
